# 上传照片到熊熊家网页

这份说明给上传照片的人看。按下面步骤操作即可，不需要改代码。

## 上传前准备

1. 准备好要上传的照片。
2. 为每一张照片提前生成或准备好对应的卡通小卡片。
3. 原照片和卡通小卡片都建议使用 JPG 或 PNG。
4. 如果照片是 HEIC，先在手机或电脑里转成 JPG。
5. 一次最好上传同一主题的一批照片，比如“滑雪”“在家”“出去玩”。
6. 如果一批照片混合了很多场景，也可以上传，分类选择“自动识别”。

最重要的规则：

- 原照片和卡通小卡片数量必须一致。
- 上传顺序必须一致。
- 第 1 张小卡片对应第 1 张原照片，第 2 张对应第 2 张，以此类推。

## 第一步：打开上传入口

1. 打开项目的 GitHub 仓库页面。
2. 点击页面上方的 `Issues`。
3. 点击绿色按钮 `New issue`。
4. 找到“照片自动更新”。
5. 点击这一项右侧的 `Get started`。

## 第二步：填写标题

在标题里写清楚这一批照片是什么，例如：

- `照片更新：2026 春天滑雪`
- `照片更新：周末在家做饭`
- `照片更新：出去玩的小照片`

标题不用很长，只要以后能看懂这一批照片的来源即可。

## 第三步：选择照片分类

在“照片分类”里选择一个选项：

- 如果这一批照片都是同一类，直接选择对应分类。
- 如果不确定，选择“自动识别”。

分类含义：

- `居家日常 home`：在家、房间、日常自拍。
- `滑雪和雪地 ski`：滑雪、雪景、冬天、雪山。
- `生病和陪伴 hospital`：医院、生病、陪伴照顾。
- `视频里的见面 video`：视频截图、屏幕里的合照。
- `娱乐时间 games`：游戏、电影、娱乐片段。
- `美厨娘时间 cooking`：做饭、吃饭、美食。
- `一起出去玩 outing`：出门、旅行、逛街、景点。
- `其他小瞬间 moments`：不好归类但想保留的照片。

## 第四步：填写照片说明

“照片说明”可以不填，但建议简单写一句。

可以写：

- 这批照片是什么时候拍的。
- 是在哪里拍的。
- 想保留什么语气或配文。
- 哪几张比较重要。

例如：

```text
这批是我们去滑雪路上和雪场拍的照片，整体放到滑雪分类里。
```

或者：

```text
这是周末在家做饭和吃饭的照片，希望配文温柔一点。
```

## 第五步：上传原照片

1. 找到“原照片附件”输入框。
2. 把原照片文件直接拖到这个输入框里。
3. 等 GitHub 上传完成。
4. 上传完成后，输入框里会出现类似 `![Image](https://github.com/user-attachments/...)` 的链接。
5. 确认每张照片都已经出现链接后，再提交。

## 第六步：上传卡通小卡片

1. 找到“卡通小卡片附件”输入框。
2. 把已经生成好的卡通小卡片拖到这个输入框里。
3. 等 GitHub 上传完成。
4. 确认小卡片数量和原照片数量一致。
5. 确认顺序一致：第 1 张小卡片对应第 1 张原照片。

注意：

- 不要只写文件名，必须真的把图片拖进去。
- 不要在图片还没上传完成时提交。
- 一次上传太多大图可能会比较慢，建议一批控制在几十张以内。

## 第七步：提交

1. 检查标题、分类、说明、原照片链接和小卡片链接。
2. 点击 `Submit new issue`。
3. 提交后不用再手动改网页。

## 第八步：等待自动处理

提交 issue 后，系统会自动做这些事：

1. 下载你上传的原照片和卡通小卡片。
2. 按分类保存到网页照片目录。
3. 压缩照片，避免网页太慢。
4. 整理并保存你上传的小卡片。
5. 更新网页照片清单。
6. 提交到 GitHub 仓库。
7. 等 GitHub Pages 自动重新发布。

通常需要几分钟。如果照片很多，可能会更久。

## 第九步：确认是否成功

可以用两种方式确认：

1. 回到刚才提交的 issue，查看系统有没有自动留言。
2. 等 GitHub Pages 更新后，打开网页查看照片墙和小卡片区。

如果成功，issue 里会出现一条“照片自动更新结果”，里面会列出新增照片和小卡片路径。

## 如果没有更新

先检查这几件事：

1. issue 里是否真的有原照片链接和小卡片链接。
2. 原照片和小卡片数量是否一致。
3. 图片是否是 JPG 或 PNG。
4. 仓库的 `Actions` 页面里，`Photo Upload Auto Update` 是否运行失败。
5. 如果失败，点进去看红色报错信息。

常见原因：

- 照片没有上传完成就提交了。
- 上传的是 HEIC，系统无法直接处理。
- GitHub Actions 没有写入仓库的权限。
- 上传的图片链接不是 GitHub 附件链接。
- 原照片和小卡片数量不一致。
- 小卡片顺序和原照片顺序不一致。

如果分类不准，可以重新上传一批，并在“照片分类”里手动指定类别；也可以让维护者手动调整 `script.js` 里的分类和配文。

## 上传建议

- 同一批照片尽量主题一致。
- 照片说明写得越清楚，自动分类和配文越容易准确。
- 先在本地检查小卡片是否满意，再上传到 GitHub。
- 不要上传重复照片。
- 不要上传特别模糊、极暗或无关的图片。
- 私密照片上传前先确认是否适合放到公开网页。
